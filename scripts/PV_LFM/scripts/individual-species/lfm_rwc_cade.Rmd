---
title: "RWC x LFM"
author: "Indra Boving"
date: "10/24/2021"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo=FALSE, message=FALSE, results='hide', warning= FALSE)
options(knitr.duplicate.label = "allow")
library(here)
library(janitor)
library(tidyverse)
library(plotly)
library(DT)
library(broom)
library(lubridate)
library(udpipe)
library(equatiomatic)
```

# Section 1: Pressure Volume Curves

```{r}
data <- read_csv(here("data", "processed-data", "allcurves_clean.csv"), show_col_types = FALSE) %>% 
  mutate(sample = as.factor(sample)) %>% 
  mutate(month = as.factor(month)) %>% 
  mutate(date = date(date)) 
```
 
### 1.1: Variation in TLPs
 
```{r}
pv.CADE.all <- data %>% 
  filter(spp == "CADE")

ggplot(pv.CADE.all) +
  geom_point(aes(x = rwd, y = -1/water_potential, color = as.factor(date)), size = .5, alpha = .5) +
  theme_light() +
  labs(x = "Relative Water Deficit", 
       y = "-1/Water potential")

pv.CADE.fall.summary <- data %>%
  group_by(date) %>% 
  filter(spp == "CADE" & timing == "fall" ) %>% 
  summarise(n=n(), mean=mean(tlp),sd=sd(tlp))
```
1. All dates: 

```{r}
pv.CADE.summary.date <- data %>%
  group_by(date) %>% 
  filter(spp == "CADE") %>% 
  summarise(n=n(), mean=mean(tlp),sd=sd(tlp))

ggplot(pv.CADE.summary.date) +
    geom_bar(aes(y = mean, x = date), position="dodge", stat="identity", fill = "lightblue", alpha = .5) +
    geom_errorbar(aes(x=date, ymin=mean-sd, ymax=mean+sd), width=0.4, colour="black", alpha=0.9) +
   labs(title = "CADE TLP, all dates") +
   theme_light()
```

```{r}

```

2. In fall and spring: 
(fall both sampling dates combined)

```{r}
pv.CADE.summary.timing <- data %>%
  group_by(timing) %>% 
  filter(spp == "CADE") %>% 
  summarise(n=n(), mean=mean(tlp),sd=sd(tlp))

 ggplot(pv.CADE.summary.timing) +
    geom_bar(aes(y = mean, x = timing), position="dodge", stat="identity", fill = "lightblue", alpha = .5) +
    geom_errorbar(aes(x=timing, ymin=mean-sd, ymax=mean+sd), width=0.4, colour="black", alpha=0.9) +
   labs(title = "CADE TLP, all dates", 
        y = "TLP (-Mpa)", 
        x = "timing") +
   theme_light()
```

```{r, include = TRUE}
#See if sig. difference in TLP:
#Split df into groups, then pull sample vectors
fall.CADE <- pv.CADE.all %>% 
  filter(timing == "fall") %>% 
  filter(year == 2021) %>% 
  group_by(sample) %>% 
  arrange(water_potential) %>%
  filter(row_number()==1) %>% 
  pull(tlp)

spring.CADE <- pv.CADE.all %>% 
  filter(timing == "spring") %>% 
   group_by(sample) %>% 
  arrange(water_potential) %>%
  filter(row_number()==1) %>% 
  pull(tlp)

CADE_tlp_t <- t.test(spring.CADE, fall.CADE)
CADE_tlp_t
```

There is significant difference betwwen TLPs of different seasons when seasons are combined (p = 0.004). 

```{r}
CADE.tlp.test <- aov(tlp ~ as.factor(date), pv.CADE.all)
TukeyHSD(CADE.tlp.test)
```


### 1.2: Try to predict Mpa from LFM with just one species

1.  Get datasets in order:

-   select for just the species and date we are interested in
-   remove any anomalous curves
-   pull the TLP as a value
-   make two sub-datasets: one for all points above the TLP, one for all points below the TLP

```{r message=FALSE, warnings=F}
pv.CADE.fall <- data %>%
  filter(spp == "CADE" & timing == "fall" ) %>% 
  filter(sample != 3)

CADE.tlp <- pv.CADE.fall %>% 
  summarise(mean(tlp)) %>% pull()

CADE.sd <- pv.CADE.fall %>% 
  summarise(sd(tlp)) %>% pull()
```

For fall PV curves, the mean TLP = `r round(CADE.tlp,2)`, sd = `r round(CADE.sd,2)`

```{r, echo=FALSE, message=FALSE, results='hide'}
pv.CADE.fall %>% 
  ggplot(aes(y = water_potential, x = lfm, color = as.factor(date))) +
  geom_point(alpha = .7, size = .7) +
  geom_hline(yintercept = CADE.tlp,
             colour = "lightblue") +
  labs(title = "CADE Pressure Volume Curves, LFM x Mpa", 
       x = "LFM (%)", y = "Mpa") +
  theme_light()
```


```{r}
#Subset data based on TLP: 

CADE.above.tlp <- pv.CADE.fall %>% 
  filter(water_potential > tlp) 
# %>% 
#   filter(lfm < 200, lfm > 90)

CADE.below.tlp <- pv.CADE.fall %>% 
  filter(water_potential < tlp)
```

2.  For points below the TLP, there should be a linear relationship between -1/Mpa and LFM

```{r}
CADE.below.tlp %>% 
ggplot(aes(y = -1/water_potential, x = lfm, color = as.factor(sample), shape = as.factor(month))) +
  geom_jitter(alpha = .6, size = 1) +
  geom_smooth(method = "lm", se = FALSE, size = .5, alpha = .5) +
  labs(y = "-1/Mpa", x = "LFM (%)") +
  theme_light()

fit.below.CADE <-lm(-1/water_potential~lfm, CADE.below.tlp)
summary(fit.below.CADE)
plot(fit.below.CADE)
```

3.  For points above the TLP, there should be a linear relationship between Mpa and lfm

```{r}
ggplot(data = CADE.above.tlp, aes(y = water_potential, x = lfm, color = sample, shape = as.factor(date))) +
  geom_jitter(alpha = .6, size = 1) +
  geom_smooth(method = "lm", se = FALSE, size = .5, alpha = .5) +
labs(y = "Mpa", x = "LFM (%)")+
  theme_light()

fit.above.CADE <- lm(water_potential ~ lfm, CADE.above.tlp)
summary(fit.above.CADE)
plot(fit.above.CADE)
```


```{r}
#Adding in SWC might help model?
fit2 <- lm(water_potential ~ lfm + swc, CADE.above.tlp)
summary(fit2)

lfm.predicted.r <- predict.lm(fit2, newdata = data_frame(lfm = 150, swc = 1.3)) %>% as.numeric() #use this to extract lfm
lfm.predicted.r
```

We have some linear models that should be able to predict Mpa from LFM. Now, to test it out...

# Section 2: Can we predict actual values using the models built from the PV curves?

## 2.1: Flam-curve values

- Read in flam curve data

```{r}
flam_curve_phys_all <- read_csv(here("data", "processed-data", "flam_curve_phys_all.csv"), show_col_types = FALSE)
```

- Use `predict()` to see if we can use PV curve models to get values collected during flammability curve testing. 

```{r, results='asis'}
#Our models: 
pretty1 <- extract_eq(fit.below.CADE)
pretty1
pretty2 <- extract_eq(fit.above.CADE)
pretty2
```

We're just interested in CADE, so just look there: 

```{r}
flam_curve_phys_CADE_above <- flam_curve_phys_all %>% 
  filter(rwc_new < 100) %>% 
  filter(spp == "CADE") %>% 
  filter(mpa > tlp)

flam_curve_phys_CADE_below <- flam_curve_phys_all %>% 
  filter(rwc_new < 100) %>% 
  filter(spp == "CADE") %>% 
  filter(mpa < tlp)
```

### Below TLP:

```{r}
predict.below <- predict(fit.below.CADE, newdata = flam_curve_phys_CADE_below, se.fit = TRUE, interval = "confidence") #make prediction

# Bind to the data to make it actually useful:

predict.df.below <- data.frame(flam_curve_phys_CADE_below, predict.below)

predict.df.below %>% 
  ggplot(aes(y = -1/water_potential, x = fit.fit)) +
  geom_point() +
  geom_abline(method = lm) +
  geom_abline(color = "red") +
  ylim(0, 1) +
  xlim(0, 1) +
  labs(title = "CADE. Actual vs. predicted -1/Mpa for points BELOW the TLP", 
       y = "Actual -1/Mpa", 
       x = "Predicted -1/Mpa") +
   theme_light() 
```


```{r}
predict.df.below %>% 
  ggplot(aes(y = water_potential, x = -1/fit.fit)) +
  geom_point() +
  geom_abline(method = lm) +
  geom_abline(color = "red") +
  ylim(0, -5) +
  xlim(0, -5) +
  labs(title = "CADE. Actual vs. predicted water potential for points BELOW the TLP", 
       y = "Actual Mpa", 
       x = "Predicted Mpa") +
   theme_light() 
```


```{r, echo = FALSE, messages = "hide", warning = FALSE}
predict_graph_below <- ggplot(predict.df.below, aes(x = lfm, y = -1/fit.fit)) +
  geom_point(color = "lightblue", alpha = 0.7) +
  geom_point(data = flam_curve_phys_CADE_below, aes(x = lfm, y = mpa), alpha = 0.7) +
 # facet_wrap(~St) +
  labs(x = "LFM (%)", 
       y = "Water Potential (Mpa)", 
       title = "Predicted (lightblue) vs. observed (black) LFM and Mpa for point BELOW the TLP (CADE)") +
 # scale_x_continuous(limits = c(500,3500), breaks = seq(500, 3500, by = 1000)) +
 # scale_y_continuous(limits = c(0,1.5e6))
  theme_light() 
predict_graph_below
```

### Above TLP:

```{r}
predict.above <- predict(fit.above.CADE, newdata = flam_curve_phys_CADE_above, se.fit = TRUE, interval = "confidence") #make prediction

# Bind to the data to make it actually useful:

predict.df.above <- data.frame(flam_curve_phys_CADE_above, predict.above)

predict.df.above %>% 
  ggplot(aes(y = water_potential, x = fit.fit)) +
  geom_point() +
  geom_abline(method = lm) +
  geom_abline(color = "red") +
  ylim(0, -3) +
  xlim(0, -3) +
  labs(title = "Actual vs. predicted water potential for points ABOVE the TLP", 
       y = "Actual Mpa", 
       x = "Predicted Mpa") +
   theme_light() 
```

```{r, echo = FALSE, messages = "hide", warning = FALSE}
predict_graph_above <- ggplot(predict.df.above, aes(x = lfm, y = fit.fit)) +
  geom_point(color = "lightblue", alpha = 0.7) +
  geom_point(data = flam_curve_phys_CADE_above, aes(x = lfm, y = mpa), alpha = 0.7) +
 # facet_wrap(~St) +
  labs(x = "LFM (%)", 
       y = "Water Potential (Mpa)", 
       title = "Predicted (lightblue) vs. observed (black) LFM and Mpa for points ABOVE the TLP (CADE)") +
 # scale_x_continuous(limits = c(500,3500), breaks = seq(500, 3500, by = 1000)) +
 # scale_y_continuous(limits = c(0,1.5e6))
  theme_light() 
predict_graph_above
```

## 2.2: Field data

- Use `predict()` to see if we can use PV curve models to get values collected in the field (predict midday Mpa from LFM): 

```{r}
field_data_CADE <- read_csv(here("data", "processed-data", "field_data.csv"), show_col_types = FALSE) %>% 
  filter(spp == "CADE")
```

We're just interested in CADE, so just look there: 

```{r}
field_data_CADE_above <- field_data_CADE %>% 
  filter(water_potential > tlp)

field_data_CADE_below <- field_data_CADE %>% 
  filter(water_potential < tlp)
```


### Below TLP:

```{r}
predict.below <- predict(fit.below.CADE, newdata = field_data_CADE_below, se.fit = TRUE, interval = "confidence") #make prediction

# Bind to the data to make it actually useful:

predict.df.below <- data.frame(field_data_CADE_below, predict.below)

predict.df.below %>% 
  ggplot(aes(y = -1/water_potential, x = fit.fit)) +
  geom_point() +
  geom_abline(method = lm) +
  geom_abline(color = "red") +
  ylim(0, 1) +
  xlim(0, 1) +
  labs(title = "Actual vs. predicted water potential for points BELOW the TLP (CADE)", 
       y = "Actual -1/Mpa", 
       x = "Predicted -1/Mpa") +
   theme_light() 

predict.df.below %>% 
  ggplot(aes(y = water_potential, x = -1/fit.fit)) +
  geom_point() +
  geom_abline(method = lm) +
  geom_abline(color = "red") +
  ylim(0, -5) +
  xlim(0, -5) +
  labs(title = "Actual vs. predicted for points BELOW TLP (CADE)", 
       y = "Actual Mpa", 
       x = "Predicted Mpa") +
   theme_light() 
```


```{r, echo = FALSE, messages = "hide", warning = FALSE}
predict_graph_below <- ggplot(predict.df.below, aes(x = lfm, y = -1/fit.fit)) +
  geom_point(color = "lightblue", alpha = 0.7) +
  geom_point(data = field_data_CADE_below, aes(x = lfm, y = water_potential), alpha = 0.7) +
 # facet_wrap(~St) +
  labs(x = "LFM (%)", 
       y = "Water Potential (Mpa)", 
       title = "Predicted (lightblue) vs. observed (black) LFM and Mpa BELOW TLP (CADE)") +
 # scale_x_continuous(limits = c(500,3500), breaks = seq(500, 3500, by = 1000)) +
 # scale_y_continuous(limits = c(0,1.5e6))
  theme_light() 
predict_graph_below
```

### Above TLP:

```{r}
predict.above <- predict(fit.above.CADE, newdata = field_data_CADE_above, se.fit = TRUE, interval = "confidence") #make prediction

# Bind to the data to make it actually useful:

predict.df.above <- data.frame(field_data_CADE_above, predict.above)

predict.df.above %>% 
  ggplot(aes(y = water_potential, x = fit.fit)) +
  geom_point() +
  geom_abline(method = lm) +
  geom_abline(color = "red") +
  ylim(0, -3) +
  xlim(0, -3) +
  labs(title = "Actual vs. predicted water potential for points ABOVE the TLP (CADE)", 
       y = "Actual Mpa", 
       x = "Predicted Mpa") +
 theme_light() 
```

```{r, echo = FALSE, messages = "hide", warning = FALSE}
predict_graph_above <- ggplot(predict.df.above, aes(x = lfm, y = fit.fit)) +
  geom_point(color = "lightblue", alpha = 0.7) +
  geom_point(data = field_data_CADE_above, aes(x = lfm, y = water_potential), alpha = 0.7) +
 # facet_wrap(~St) +
  labs(x = "LFM (%)", 
       y = "Water Potential (Mpa)", 
       title = "Predicted (lightblue) vs. observed (black) LFM and Mpa for points ABOVE the TLP (CADE)")+
 # scale_x_continuous(limits = c(500,3500), breaks = seq(500, 3500, by = 1000)) +
 # scale_y_continuous(limits = c(0,1.5e6))
  theme_light() 
predict_graph_above
```
